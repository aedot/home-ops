---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./grafanadashboard.yaml
  # Vendored from https://raw.githubusercontent.com/dragonflydb/dragonfly-operator/v1.6.1/manifests/crd.yaml
  # Keep in sync with the operator image tag in helmrelease.yaml; re-fetch on version bump.
  - ./crds.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./rbac.yaml

---
clusterName: k8s-gitops

talosVersion: "${talosVersion}"
kubernetesVersion: "${kubernetesVersion}"

endpoint: https://172.24.20.11:6443
additionalApiServerCertSans: &sans
  - "127.0.0.1"
  - "172.24.20.11"
additionalMachineCertSans: *sans

clusterPodNets: ["10.42.0.0/16"]
clusterSvcNets: ["10.43.0.0/16"]

# Disable built-in CNI to use Cilium
cniConfig:
  name: none

nodes:
  - hostname: "oya"
    ipAddress: "172.24.20.12"
    installDiskSelector:
      serial: S46YNA0M907053
    machineSpec:
      secureboot: false
    talosImageURL: factory.talos.dev/metal-installer/688503147151b0e7d97ee8e1ea87a44ae76beafb62878764b28e179b06c61a28
    controlPlane: true
    networkInterfaces:
      - interface: bond0
        bond:
          deviceSelectors:
            - hardwareAddr: "58:47:ca:7b:e6:e4"
          mode: active-backup
        dhcp: true
        vlans: &vlans
          - # IoT VLAN
            { vlanId: 40, dhcp: false, mtu: 1500 }
          - # Vpn VLAN
            { vlanId: 50, dhcp: false, mtu: 1500}
        vip:
          ip: &vip "172.24.20.11"

  - hostname: "aje"
    ipAddress: "172.24.20.13"
    installDiskSelector:
      serial: S46YNA0M906633
    machineSpec:
      secureboot: false
    talosImageURL: factory.talos.dev/metal-installer/688503147151b0e7d97ee8e1ea87a44ae76beafb62878764b28e179b06c61a28
    controlPlane: true
    networkInterfaces:
      - interface: bond0
        bond:
          deviceSelectors:
            - hardwareAddr: "58:47:ca:7b:e9:48"
          mode: active-backup
        dhcp: true
        vip:
          ip: *vip
        vlans: *vlans

  - hostname: "oba"
    ipAddress: "172.24.20.14"
    installDiskSelector:
      serial: S46YNA0M907048
    machineSpec:
      secureboot: false
    talosImageURL: factory.talos.dev/metal-installer/688503147151b0e7d97ee8e1ea87a44ae76beafb62878764b28e179b06c61a28
    controlPlane: true
    networkInterfaces:
      - interface: bond0
        bond:
          deviceSelectors:
            - hardwareAddr: "58:47:ca:7b:ea:1c"
          mode: active-backup
        dhcp: true
        vip:
          ip: *vip
        vlans: *vlans

# Global patches
patches:
  - "@./patches/global/machine-features.yaml"
  - "@./patches/global/machine-files.yaml"
  - "@./patches/global/machine-kernel.yaml"
  - "@./patches/global/machine-kubelet.yaml"
  - "@./patches/global/machine-network.yaml"
  - "@./patches/global/machine-sysctls.yaml"
  - "@./patches/global/machine-time.yaml"
  # - "@./patches/global/machine-udev.yaml"
  - "@./patches/global/machine-nodelabel.yaml"
  - "@./patches/global/longhorn-v2-volume.yaml"

# Controller patches
controlPlane:
  patches:
    - "@./patches/controller/cluster.yaml"

  schematic:
    customization:
      extraKernelArgs:
        - init_on_alloc=0                     # Less security, faster puter
        - init_on_free=0                      # Less security, faster puter
        - intel_iommu=on                      # PCI Passthrough
        - iommu=pt                            # PCI Passthrough
        - mitigations=off                     # Less security, faster puter
        - security=none                       # Less security, faster puter
        - talos.auditd.disabled=1             # Less security, faster puter
        - i915.enable_guc=3                   # Enable GuC and HuC for better graphics performance and stability
        - i915.force_probe=!a7a0              # Disable iGPU deviceID for i915
        - xe.force_probe=a7a0                 # Enable iGPU deviceID for xe
      systemExtensions:
        officialExtensions:
          - siderolabs/i915
          - siderolabs/intel-ucode
          - siderolabs/iscsi-tools
          - siderolabs/mei
          - siderolabs/nfsrahead
          - siderolabs/thunderbolt
          - siderolabs/util-linux-tools
          - siderolabs/xe

